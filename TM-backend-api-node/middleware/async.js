module.exports = function (handler) {
  return async (req, res, next) => {
    try {
      await handler(req, res);
    } catch (ex) {
      next(ex);
    }
  };
};
//(npm i express-async-errors used to remove asyncMiddleware name and make it as async)
